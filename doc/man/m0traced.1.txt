M0TRACEd(1)
===========
include::asciidoc.conf[]


NAME
----
m0traced - daemon, which polls trace buffer of m0mero.ko or m0d and saves trace
records into continuous trace file


SYNOPSIS
--------
*m0traced* ['OPTIONS']


DESCRIPTION
-----------
Daemon, which polls trace buffer of *m0mero.ko* or *m0d* and saves trace records
in binary format into continuous trace file for postmortem analysis by
*m0tracedump* and *m0trace* utilities. It supports log rotation when log size
reaches some threshold.

OPTIONS
-------
*-i* 'IN_FILE'::
    Input file name, if none is provided, then `/sys/kernel/debug/mero/trace/buffer`
    is used by default.

*-o* 'OUT_FILE'::
    Output file name, if none is provided, then `/var/log/mero/m0trace.bin` is
    used by default.

*-d*::
    Daemon mode (run in the background, log errors into syslog).

*-l*::
    Log level number (from syslog.h).

*-s*::
    Size of output trace file in MB, when it's reached, log rotation is
    performed; if set to 0 then rotation is disabled, default is 1024MB.

*-k*::
    Number of logs to keep in log rotation, default is 6.

*-h, --help*::
    Display help screen and exit.


EXAMPLES
--------
Run *m0traced* with default settings:

  $ m0traced

Run as daemon and save traces of *m0d* process with PID 33972:

  $ m0traced -d -i /var/run/mero/m0trace.33972 -o /var/log/mero/m0trace.33972.bin


AUTHORS
-------
See the Mero distribution AUTHORS file.


BUGS
----
All bugs are added by Mero Team ;)


RESOURCES
---------
Main web site: <http://xyratex.com/>


COPYING
-------
Copyright \(C) 2013 Xyratex Technology Limited

You should have received a copy of Xyratex's license along with
this release. If not please contact a Xyratex representative.


SEE ALSO
--------
*m0trace*(1) *m0tracedump*(1) *mero*(7)


MERO
----
Part of the *mero*(7) suite


// vim: syntax=asciidoc
