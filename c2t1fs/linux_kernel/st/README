Mannual test for c2t1fs with BulkIO server.

This test creates the stobs for specified pool width. And run different
combinations of I/O for following parameters :
1. stripe_size    : 4K to 32K
2. I/O size       : multiple of stripe_size (Small in KBs & Large in MBs)
                    8 samples each.
3. Number of I/Os : 1 & 2

Steps to test c2t1fs with BulkIO server :

0. Set up test environment

bash#. setupenv.sh

1. Create the stobs for specified pool width

bash#./stobsetup.sh <pool_width>
   
2. Start colibri I/O service 

bash#./colibri_server start

3. Run different combinations of I/O request for small & large data

bash#./c2t1fs_bulkio.sh <pool_width> <data_units> <parity_units>

4. Stop colibri I/O service

bash#./colibri_server stop

