From 228373a2acaa8e561b119d9043222b4b805d195f Mon Sep 17 00:00:00 2001
From: Maxim Medved <max_medved@xyratex.com>
Date: Mon, 4 Nov 2013 18:08:09 +0200
Subject: [PATCH 2/2] testing: copied yaml config file

---
 tmp/mero-single_tests.yaml | 39 +++++++++++++++++++++++++++++++++++++++
 tmp/mero_testing.pl        |  2 +-
 2 files changed, 40 insertions(+), 1 deletion(-)
 create mode 100644 tmp/mero-single_tests.yaml

diff --git a/tmp/mero-single_tests.yaml b/tmp/mero-single_tests.yaml
new file mode 100644
index 0000000..aedb76b
--- /dev/null
+++ b/tmp/mero-single_tests.yaml
@@ -0,0 +1,39 @@
+--- 
+Tests: 
+  - id       : 0userspace-tests
+    script   : ut.sh
+    dir      : src/utils/
+    executor : colibritests::KernelTest
+    polltime : 15
+    timeout  : 900    
+
+  - id       : 1kernel-tests
+    script   : ut.sh
+    dir      : src/utils/linux_kernel/
+    executor : colibritests::KernelTest
+    polltime : 15
+
+#  - id       : crash-node
+#    script   : modinfo ; /bin/echo > > /proc/sysrq-trigger
+#    dir      : /
+#    executor : colibritests::KernelTest
+#    polltime : 30
+#    builddir : /sbin/
+
+  - id       : 2system-tests
+    script   : m0t1fs_test.sh
+    dir      : src/m0t1fs/linux_kernel/st/
+    executor : colibritests::KernelTest
+    polltime : 30
+    timeout  : 2000
+# 25 min
+dangerous: 'yes'
+description: Set of Mero  tests
+expected_time: 300
+groupname: mero-single
+reference: none
+roles: StoreSyslog NetconsoleCollector 
+schema: data/schemas/testds.yaml
+tags: mero 
+timeout: 300
+
diff --git a/tmp/mero_testing.pl b/tmp/mero_testing.pl
index 38ccee2..2a597d1 100644
--- a/tmp/mero_testing.pl
+++ b/tmp/mero_testing.pl
@@ -208,7 +208,7 @@ ILIST
     #mkdir 'tests';
     #my $excludelist = $EXCLUDELIST if defined $EXCLUDELIST;
     run( "rm -rf $wd/tmpwd && mkdir $wd/tmpwd",                             1 );
-    run( "cp $wd/scripts/colibritests/colibri-single_tests.yaml $wd/tmpwd", 1 );
+    run( "cp $wd/workdir/src/tmp/mero-single_tests.yaml $wd/tmpwd", 1 );
     if ( defined($OVERRIDEROLES) ) {
         WARN "Applying roles [$OVERRIDEROLES]";
         $CWD = $XPERIORHOME;
-- 
1.8.3.2

