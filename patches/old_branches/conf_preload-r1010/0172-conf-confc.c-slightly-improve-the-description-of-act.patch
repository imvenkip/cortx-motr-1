From 73c4cd8ab0480357d6b4d22755ccecdc5b4275fa Mon Sep 17 00:00:00 2001
From: "Valery V. Vorotyntsev" <valery_vorotyntsev@xyratex.com>
Date: Sat, 12 May 2012 01:24:50 +0300
Subject: [PATCH 172/311] conf/confc.c: slightly improve the description of
 act()

LogD: #1191
RB: r/714
---
 conf/confc.c | 7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

diff --git a/conf/confc.c b/conf/confc.c
index 5c6360c..9573331 100644
--- a/conf/confc.c
+++ b/conf/confc.c
@@ -456,10 +456,11 @@ static int walk(struct walk_ctx *wx)
  * <b>2. C2_CS_MISSING</b>
  *
  * act() fills configuration fetch request (c2_confc_ctx::fc_req) and
- * sets status of the object reached by walk() to C2_CS_LOADING. If a
- * directory is reached, act() C2_ASSERT()s that there are no
+ * sets status of the object reached by walk() to C2_CS_LOADING.  Or,
+ * if a directory is reached, act() C2_ASSERT()s that there are no
  * C2_CS_MISSING objects in this directory and that the number of
- * C2_CS_LOADING objects is > 0.
+ * C2_CS_LOADING objects is > 0 (thus verifying that walk() did its
+ * job properly).
  *
  * <b>3. Single C2_CS_LOADING object</b>
  *
-- 
1.8.3.2

