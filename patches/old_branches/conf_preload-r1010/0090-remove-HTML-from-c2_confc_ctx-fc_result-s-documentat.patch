From 21404e99a9c1bd1b3876d654c4e6f56600130cfd Mon Sep 17 00:00:00 2001
From: "Valery V. Vorotyntsev" <valery_vorotyntsev@xyratex.com>
Date: Fri, 23 Mar 2012 12:06:45 +0200
Subject: [PATCH 090/311] remove HTML from c2_confc_ctx::fc_result's
 documentation

---
 conf/confc.h | 13 ++++++-------
 1 file changed, 6 insertions(+), 7 deletions(-)

diff --git a/conf/confc.h b/conf/confc.h
index 31fedf7..a1feff9 100644
--- a/conf/confc.h
+++ b/conf/confc.h
@@ -369,15 +369,14 @@ struct c2_confc_ctx {
 	 * Pointer to the requested configuration object/directory.
 	 *
 	 * To be type-cast to
-	 * - <tt>struct X *</tt>, where X is a _concrete_ configuration
-	 *   object type (c2_conf_profile, c2_conf_filesystem, etc.),
-	 *   if the field is filled by c2_confc_open() call;
-	 * - <tt>struct c2_confc_dir *</tt> when filled by
-	 *   c2_confc_diropen().
+	 * - `struct X *', where X is a _concrete_ configuration
+	 *   object type (c2_conf_profile, c2_conf_filesystem, etc.)
+	 *   in case of c2_confc_open();
+	 * - `struct c2_confc_dir *' in case of c2_confc_diropen().
 	 *
 	 * The value is NULL until ->fc_complete channel is signaled.
-	 * It is also NULL if configuration retrieval failed (see
-	 * c2_confc_error()).
+	 * It is also NULL if configuration retrieval failed; see
+	 * c2_confc_error().
 	 */
 	void                *fc_result;
 /* private: internal use only */
-- 
1.8.3.2

