From 46b4cec49bfefac629a5fa5d32a20c709fb5f7be Mon Sep 17 00:00:00 2001
From: Yuriy Umanets <yuriy_umanets@xyratex.com>
Date: Thu, 14 Apr 2011 06:05:47 -0600
Subject: [PATCH 063/177] - some cleanups

---
 reqh/reqh.c | 10 +++++-----
 site/site.c |  3 ++-
 site/site.h |  6 +++++-
 3 files changed, 12 insertions(+), 7 deletions(-)

diff --git a/reqh/reqh.c b/reqh/reqh.c
index ce607f1..783f5bf 100644
--- a/reqh/reqh.c
+++ b/reqh/reqh.c
@@ -37,11 +37,11 @@ void c2_reqh_fini(struct c2_reqh *reqh)
 
 int c2_reqh_fop_handle(struct c2_reqh *reqh, struct c2_fop *fop, void *cookie)
 {
-	struct c2_fop_ctx 	ctx;
-	int			result;
-        struct c2_fop_env       env;
-        struct c2_dtx           tx;
-	struct c2_fom	       *fom = NULL;
+	struct c2_fop_ctx    ctx;
+        struct c2_fop_env    env;
+        struct c2_dtx        tx;
+	struct c2_fom	    *fom = NULL;
+	int                  result;
 
         C2_SET0(&ctx);
         C2_SET0(&env);
diff --git a/site/site.c b/site/site.c
index 26e16b9..8fbb83c 100644
--- a/site/site.c
+++ b/site/site.c
@@ -27,7 +27,8 @@
 
 static struct c2_list c2_sites;
  
-int c2_site_init(struct c2_site *s, struct c2_md_store *md, 
+int c2_site_init(struct c2_site *s, 
+                 struct c2_md_store *md, 
                  struct c2_data_store *data)
 {
         s->s_mdstore = md;
diff --git a/site/site.h b/site/site.h
index df038a7..54aa95e 100644
--- a/site/site.h
+++ b/site/site.h
@@ -20,7 +20,11 @@ struct c2_site {
         struct c2_list_link     s_linkage;
 };
 
-int c2_site_init(struct c2_site *s, struct c2_md_store *md, 
+/**
+   Init site using metadata and data stores.
+ */
+int c2_site_init(struct c2_site *s, 
+                 struct c2_md_store *md, 
                  struct c2_data_store *data);
 
 void c2_site_fini(struct c2_site *s);
-- 
1.8.3.2

